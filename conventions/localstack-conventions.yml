generated: '2026-07-20'
method: derived
source: >-
  openapi/localstack-openapi-original.yml, https://docs.localstack.cloud/references/internal-endpoints/
authentication:
  style: none-for-local-rest-api
  ref: authentication/localstack-authentication.yml
base_url: http://localhost.localstack.cloud:4566
version_signaling:
  response_header: x-localstack
  detail: >-
    Every response carries an `x-localstack` header containing the LocalStack version
    (e.g. 2026.3.1.dev65), letting a client detect LocalStack and its version in one request.
  toggle_env: LOCALSTACK_RESPONSE_HEADER_ENABLED
idempotency:
  supported: false
  detail: No idempotency-key header/parameter is documented or present in the OpenAPI.
pagination:
  supported: false
  detail: >-
    The retrospective/introspection endpoints return full result sets; no cursor/offset
    pagination parameters are defined.
filtering:
  supported: true
  detail: >-
    Several endpoints accept query filters (SES id/email; SNS accountId/region/endpointArn/
    phoneNumber; Lambda runtimes filter=all|deprecated|supported).
content_negotiation:
  detail: >-
    SQS retrospective endpoints return text/xml or application/json; most others return
    application/json; a few return text/plain (health HEAD/POST) or application/octet-stream
    (Lambda init binary).
error_envelope:
  ref: errors/localstack-problem-types.yml
  detail: Plain 400/404 responses; not RFC 9457 problem+json.
rate_limiting:
  detail: No rate limiting — the API is served by the local emulator container.
discovery:
  swagger_ui: http://localhost.localstack.cloud:4566/_localstack/swagger
  api_catalog: https://docs.localstack.cloud/.well-known/api-catalog
